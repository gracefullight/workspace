name: Deploy MCP Cafe24 Callback to Vercel

on:
  push:
    branches: [main]
    paths:
      - "apps/mcp-cafe24-admin-example/**"
      - ".github/workflows/vercel.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_CAFE24_ADMIN_EXAMPLE }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_CWD: apps/mcp-cafe24-admin-example
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "10.0.0"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "pnpm"

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel environment
        run: vercel pull --yes --environment=production --token=$VERCEL_TOKEN --cwd $VERCEL_CWD

      - name: Build Vercel project
        run: vercel build --prod --token=$VERCEL_TOKEN --cwd $VERCEL_CWD

      - name: Deploy to Vercel
        run: vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN --cwd $VERCEL_CWD
