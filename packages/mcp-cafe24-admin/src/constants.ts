// Cafe24 API base URL configuration
export const CAFE24_API_BASE_URL = "https://{mall_id}.cafe24api.com/api/v2";

// Character limit for responses
export const CHARACTER_LIMIT = 25000;

// Response format options
export enum ResponseFormat {
  MARKDOWN = "markdown",
  JSON = "json",
}
